# Components 目錄指南

## 概述

此目錄用於存放可重用的 Vue 元件。這些元件通常是 UI 元素，不直接與路由關聯。

## 命名規範

- 建議使用 PascalCase 命名法 (例如 `RecipeCard.vue`, `TechNotePreview.vue`)。

## 內容範例

- `Header.vue`, `Footer.vue`
- `RecipeCard.vue`, `TechNotePreview.vue`
- `Pagination.vue`, `Modal.vue`
